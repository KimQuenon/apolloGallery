{% extends 'admin/base.html.twig' %}

{% block title %}Messagerie{% endblock %}

{% block body %}
    <div class="container full">
        <h1>Message de {{contact.firstName}}</h1>
        <p>{{contact.message}}</p>
        <a href="mailto:{{contact.email}}" type="button" class="btn btn-primary">Répondre</a>
    </div>
{% endblock %}